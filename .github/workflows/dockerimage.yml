name: Docker Image Build CI

on: [push]

jobs:

  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v1
    - name: Build the Docker image
      run: ./build-docker.sh && cd oximachine && ./build-docker.sh
      
